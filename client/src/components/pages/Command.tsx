import Command from "../add/Command";
// import Footer from "../common/Footer";
// import Navbar from "../common/navBar";
// made the separate  routes for the only command page, 
export default function CommandPage(){
  return (
    <main>
      <Command/>
    </main>
  )
}